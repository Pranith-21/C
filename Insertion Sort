#INSERTION SORT

#include<stdio.h>
#include<stdlib.h>
void insert(int b[],int n)
{   int i,t,k;
    for(i=1;i<n;i++)
    {
        t=i;
    
        while(t>0 && b[t-1]>b[t])
            {
                k=b[t-1];
                b[t-1]=b[t];
                b[t]=k;
                t=t-1;
            }
    }
    for(i=0;i<n;i++)
        printf("%d ",b[i]);
}

int main()
{   
    int n,i;
    scanf("%d",&n);
    int a[n];
    for(i=0;i<n;i++)
        scanf("%d",&a[i]);

    insert(a,n);
    return 0;
}
